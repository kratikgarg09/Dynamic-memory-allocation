#include<iostream> 
using namespace std; 

#define pi 3.14 

int main() 
{ 
    int r;
    cin >> r;
    cout << pi*r*r;
    return 0;
}

// a piece or line of a code is replaced by the value of macro. Macros is defined by #define directive.